# Sensors

Kamerplanter supports four data sources with automatic fallback: automatic (IoT/MQTT) → semi-automatic (Home Assistant REST API) → weather API (DWD/Open-Meteo for outdoor) → manual entry. Data provenance is always tracked.

!!! note "Placeholder"
    This content will be elaborated in a subsequent step.
