# Kubernetes

Kamerplanter läuft auf Kubernetes 1.28+. Für lokale Entwicklung wird Kind (Kubernetes in Docker) empfohlen.

!!! note "Platzhalter"
    Dieser Inhalt wird in einem folgenden Schritt ausgearbeitet.

## Kind-Konfiguration

Die Kind-Konfiguration liegt unter `kind-config.yaml` im Projekt-Root.
