# Approved reach probe; derived by capability-reach-scanner, approved via capability-reach-audit.
# Never edit in place: any later commit to this file is reported as weakened. Re-derive instead.
# Approval: recorded under the repository owner's standing pre-approval of every gate for the
# autonomous #1783 run (2026-09-24/25), relayed by the orchestrating session — not an interactive
# batch answer. It records that the targets and the observe argv below run on the operator's
# machine with their environment, as `task` in this repository does.
#
# Re-derived for #1783: #1780 squash-merged as c9d6d29ee, leaving its branch commit e9e0c3a52
# outside develop's history. REQ-025 is byte-identical between the two; derived_from is now the
# squash commit, the latest commit of the declaration on develop. Observe step, targets and
# expectation unchanged.
#
# Re-derived for #1770: REQ-025 v1.12 added a changelog row and AK-OS-08 / AK-OS-05b after AK-OS-07,
# so every cited line moved +1; the cited passages are unchanged. derived_from is 4f611fc1e, the commit
# that changed the declaration. Observe step, targets and expectation unchanged.
#
# Re-derived for #1770 (review round): AK-OS-08 was extended in place at 7e6f8c2a6; no cited line
# moved. Observe step, targets and expectation unchanged.
id: requirement-req025-art17-reference-index-cleanup
summary: subject's user-contributed DINOv2 embedding and the curated control row, in pgvector, after erasure through
  the real (non-no-op) binding
declaration:
  source: requirement
  path: spec/req/REQ-025_Datenschutz-Betroffenenrechte.md
  location: §3.1.1 ErasureEngine.REFERENCE_INDEX_CLEANUP_RULES (L267); §3.5 Phase 0.5 (L945-964); AK-OS-05 (L1231);
    AK-OS-05a (L1231)
tier: T2
expected:
  kind: set
  unit: pgvector species_embeddings state after erasure finalisation
  values:
  - erased/species_embeddings
  - kept/curated
derived_from: 7e6f8c2a6308ca06b641ead0433a4e55fc6020f5
environment:
- reach:stack:up
- reach:vectordb:up
- reach:seed:privacy-subject
- reach:seed:reference-embedding
- reach:erasure:finalise-for-subject
teardown:
- reach:vectordb:down
- reach:stack:down
observe:
  argv:
  - python3
  - scripts/reach/observe_reference_index.py
  - --subject
  - reach-subject
  timeout_seconds: 120
approval:
  approved_at: '2026-09-25T07:11:22Z'
  approved_by: nolte
  observation_digest: a77689fbf7ef9fe2ae0244aabfa7062084986ac427a72c6dbb7f92c8a6a90051
