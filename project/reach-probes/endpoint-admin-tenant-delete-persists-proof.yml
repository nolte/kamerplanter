# Reach probe; derived by capability-reach-scanner via capability-reach-audit.
# Never edit in place: any later commit to this file is reported as weakened. Re-derive instead.
# Approval: recorded under the repository owner's explicit approval given in the orchestrating session on 2026-09-25 (#1788 personal-tenant probes; re-derivation after #1823 with unchanged expectations).
#
# Re-derived after #1823: #1791 squash-merged as 499c890d0 and changed router.py (delete_tenant
# now takes a step-up body: confirm_slug + password, plus the api-key flag and a membership
# re-proof in TenantService.delete_tenant). derived_from is now that commit. The act script
# scripts/reach/admin_delete_tenant.py already sends the step-up; origin stays platform_admin and
# TenantErasureEngine.INVENTORY is byte-identical to 0d7276476. Observe step, targets and
# expectation unchanged (3/3 recounted at 9e64aec20).
id: endpoint-admin-tenant-delete-persists-proof
summary: tenant_erasure_records after the platform-admin tenant delete — one completed platform_admin record, none containing
  the member's account key
declaration:
  source: endpoint
  path: src/backend/app/api/v1/admin/platform/router.py
  location: DELETE /admin/platform/tenants/{key} (delete_tenant) — a tenant_erasure_records entry (origin platform_admin)
    is persisted as the proof, per TenantService.delete_tenant and the route docstring (#1769)
tier: T2
expected:
  kind: set
  unit: observed tenant-erasure record facts
  values:
  - member-in-records/0
  - record-unreached/0
  - record/platform_admin/completed
derived_from: 499c890d024c0e082d4c49a32a7ab386ca13a1e8
environment:
- reach:stack:up
- reach:seed:erasure-tenant
- reach:erasure:admin-delete-tenant
teardown:
- reach:stack:down
observe:
  argv:
  - python3
  - scripts/reach/observe_tenant_residue.py
  - --tenant
  - reach-tenant
  - --records
  timeout_seconds: 120
approval:
  observation_digest: f1df40ac229836df8c90604d3eae1f93f2e61dbe7b6b3a0730e78a3ebe752baa
  approved_by: nolte
  approved_at: '2026-09-25T15:41:31Z'
