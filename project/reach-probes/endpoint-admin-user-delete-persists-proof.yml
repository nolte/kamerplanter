# Approved reach probe; derived by capability-reach-scanner, approved via capability-reach-audit.
# Never edit in place: any later commit to this file is reported as weakened. Re-derive instead.
# Approval: recorded under the repository owner's standing pre-approval of every gate for the
# autonomous #1768 run (2026-09-24/25), relayed by the orchestrating session — not an interactive
# batch answer. It records that the targets and the observe argv below run on the operator's
# machine with their environment, as `task` in this repository does.
#
# Re-derived for #1769: router.py changed in e6af8a62d (the tenant-delete route beside this one);
# DELETE /admin/platform/users/{key}, the erasure plan's steps and this observe step are unchanged
# (erasure_engine.py changed since 77efb5095 only by #1786's log-subject helpers). Earlier:
# Re-derived for #1768: the previous derived_from 2994893c9 was a #1776 branch commit the squash
# made unreachable; the declaration last changed in the squash 77efb5095 on develop. Observe step,
# targets and expectation unchanged (64/64 recounted against erasure_engine.py at 77efb5095).
id: endpoint-admin-user-delete-persists-proof
summary: erasure records after the platform-admin user delete — one completed platform_admin record, none naming
  the subject
declaration:
  source: endpoint
  path: src/backend/app/api/v1/admin/platform/router.py
  location: DELETE /admin/platform/users/{key} (delete_user) — an erasure request (origin platform_admin) is persisted
    as the proof (#1767 GDPR-004)
tier: T2
expected:
  kind: set
  unit: observed erasure-record facts
  values:
  - record/platform_admin/completed
  - subject-in-records/0
derived_from: e6af8a62dfe41bcd50c22fbee0871ca029d73469
environment:
- reach:stack:up
- reach:seed:privacy-subject
- reach:erasure:admin-delete-subject
teardown:
- reach:stack:down
observe:
  argv:
  - python3
  - scripts/reach/observe_erasure_records.py
  - --subject
  - reach-subject
  timeout_seconds: 120
approval:
  approved_at: '2026-09-25T09:19:17Z'
  approved_by: nolte
  observation_digest: fe814f713d7729b1c4193deff99ca704d264974b061764aa33f8ec45be7eee5d
