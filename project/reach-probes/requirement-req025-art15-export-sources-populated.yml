# Approved reach probe; derived by capability-reach-scanner, approved via capability-reach-audit.
# Never edit in place: any later commit to this file is reported as weakened. Re-derive instead.
# Approval: recorded under the operator's standing pre-approval of the gate for the
# autonomous #1680 run, relayed by the orchestrating session — not an interactive
# batch answer. It records that the targets and the observe argv below run on the
# operator's machine with their environment, as `task` in this repository does.
#
# Spec row "Data export reaches 0 collections" (T2), against the requirement's own
# enumeration: the 10 collections and 4 edges REQ-025 §3.1 lists for the Art. 15/20
# manifest. The observation opens the archive the worker wrote into object storage
# and lists a collection only when a record in its section IS one of the subject's
# seeded rows (seed marker, or projection holding a value only the seed wrote); an
# archive of empty sections prints nothing. The request's status is never read.
# note: no local referent found for tenant_species_config / tenant_cultivar_config
# (grep over src/: no collection, model, repository or manifest source), so the
# seed has nothing to put there and no export can disclose it.
id: requirement-req025-art15-export-sources-populated
summary: sources REQ-025's export manifest names whose seeded rows reach the subject's Art. 15 archive
declaration:
  source: requirement
  path: spec/req/REQ-025_Datenschutz-Betroffenenrechte.md
  location: §3.1 Engine-Schicht, DataExportEngine.USER_DATA_MANIFEST (L244)
tier: T2
expected:
  kind: set
  unit: declared collections and edges disclosed with the subject's rows
  values:
  - collection/users
  - collection/auth_providers
  - collection/refresh_tokens
  - collection/memberships
  - collection/consent_records
  - collection/tasks
  - collection/harvest_batches
  - collection/inspections
  - collection/tenant_species_config
  - collection/tenant_cultivar_config
  - edge/has_auth_provider
  - edge/has_session
  - edge/has_membership
  - edge/has_consent
derived_from: 18329ac36b1222a95dbedcf34125d608701d155f
environment:
- reach:stack:up
- reach:seed:privacy-subject
- reach:export:run-for-subject
teardown:
- reach:stack:down
observe:
  argv:
  - python3
  - scripts/reach/observe_export_archive.py
  - --subject
  - reach-subject
  timeout_seconds: 300
approval:
  approved_at: '2026-09-24T04:46:06Z'
  approved_by: nolte
  observation_digest: 17281a965c090fdf0d4e4a3b73f6e4f4ab96d7690503b7c71280c6699879d8e4
