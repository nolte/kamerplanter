# Offene Issues — Umsetzungsplan (Stand 2026-09-11)

> **Nachtrag 2026-09-11, nach der Ausführung von Welle 1.** Der Plan bleibt wie
> geschrieben stehen; der Wert liegt darin, *warum* etwas anders kam. Vier Zahlen
> und zwei Annahmen aus diesem Dokument haben die Umsetzung nicht überlebt.
>
> **Welle 1 ist abgeschlossen**, in drei PRs statt einem:
>
> | Paket | Ergebnis |
> |---|---|
> | P1.1 #1385 | gemergt `868d53746` — Issue zu |
> | P1.2 #1352/#1372 | gemergt `a3ad72329` — beide Issues zu |
> | P1.3 #1353 | gemergt `5e9c8662b` — Issue zu |
>
> **Die Umfänge waren durchweg zu klein geschätzt, immer in dieselbe Richtung:**
>
> - **P1.2 nannte vier Stellen. Es sind elf.** Der Plan zählte fünf
>   über-ablehnende und eine fehlende; dazu kamen zwei *inerte* Wächter, drei
>   AQL-Projektionen und — erst von der Pre-Merge-Review gefunden — zwei weitere
>   in `nutrient_plan_service` und `fertilizer_service`, wieder je eine pro
>   Fehlerrichtung. #1397 hält die Klasse, die drei Projektionen sind offen.
> - **P1.3 nannte 37 Routen. Es sind 45.** Der AST-Sweep, aus dem #1353 seine Zahl
>   zog, schlüsselte auf den Dateinamen `tenant_router.py` und konnte drei Routen
>   nicht sehen. Dazu fünf unter `/admin`, für die der im Issue beschriebene
>   Selektor gar nicht zuständig gewesen wäre.
>
> **Zwei Sicherheitslücken sind erst beim Arbeiten entstanden — genauer: erst
> beim Arbeiten sichtbar geworden.** Beide fand der Sweep aus P1.3 auf seinem
> ersten Lauf, was sein Zweck war: **#1399** (`/admin/oidc-providers` vollständig
> ungegatet, über den OAuth-Auto-Link-Pfad eine Kontoübernahme) und **#1401**
> (`/admin/recognition`). Die zweite Hälfte von #1399 — `should_auto_link` bekommt
> ein literales `True` — steht als **#1403** und ist **nicht** behoben.
>
> **Was der Plan methodisch unterschätzt hat.** §5 verlangt, die behauptete
> Ursache nachzumessen. Das hat gegriffen. Was er nicht vorsah: dass **meine
> eigenen** Begründungen dieselbe Prüfung brauchen. Fünfmal hat eine Review einen
> Fehler in etwas gefunden, das ich gerade als geprüft abgesegnet hatte — dreimal
> ein Allowlist-Eintrag, dessen Begründung ich aus Routenname und Nachbarschaft
> statt aus dem Rumpf abgeleitet hatte, einmal ein Kommentar, den ich in zwei
> Richtungen falsch hatte, einmal ein Guard vor der Stelle, die den geprüften Wert
> danach überschreibt. Und zwei Wächter von mir trugen ihre eigene Zusage nicht:
> der Sweep war unter `KAMERPLANTER_MODE=light` halb blind und übersah die
> `getattr`-Form, die zwei echte Defekte benutzen.
>
> Für §5 heißt das eine Ergänzung, die hier fehlte: **eine Begründung, die aus
> der Lage einer Route abgeleitet ist, ist keine Messung.** Ein Allowlist-Eintrag
> braucht denselben Nachweis wie ein Gate.
>
> **Korrektur am Nachtrag selbst, 2026-09-11 nach der Pre-Merge-Review von
> #1395.** Die erste Fassung dieses Absatzes erklärte W-1 für „durch den zweiten
> Selektor aufgelöst" und ließ **#1402** ganz weg. Beides ist falsch, und es ist
> derselbe Fehler, den dieser Nachtrag zwei Absätze weiter oben bei mir selbst
> benennt: eine Zusage für erfüllt erklären, ohne sie nachzumessen.
>
> W-1s Auflösung versprach ein Gate für „**jede** schreibende Route mit einem
> nicht-öffentlichen Gate". Gemessen führt der gemergte Sweep genau zwei
> Selektoren — `/t/{tenant_slug}` und `/api/v1/admin` —, und **133 Schreibrouten
> fallen durch beide.**
>
> **Vierzehn Schreibrouten sind ohne jede Authentifizierung erreichbar**, und sie
> verteilen sich auf zwei Module, die der Sweep aus *verschiedenen* Gründen nicht
> meldet:
>
> | Modul | Routen | Warum der Sweep schweigt |
> |---|---|---|
> | `calculations/router.py` | `vpd`, `gdd`, `photoperiod-transition`, `sun-times`, `sun-times-range`, `slot-capacity`, `vernalization` | liegt unter `/api/v1/calculations` — fällt durch **beide** Selektoren |
> | `nutrient_calculations/router.py` | `mixing-protocol`, `flushing`, `runoff`, `mixing-safety`, `water-mix`, `water-mix/reverse`, `ec-budget` | liegt unter `/t/{tenant_slug}` — wird **gesehen**, aber der Selektor prüft auf ein bestimmtes `ctx`, nicht auf dessen Abwesenheit |
>
> **Die Vierzehn trägt einen Filter, und der gehört dazu.** Mit demselben Walker
> und einem transitiven Lesen von `route.dependant` lösen **24** Schreiboperationen
> überhaupt keine Auth-Dependency auf. **Zehn** davon sind bauartbedingt öffentlich:
> die Auth-Einstiegspunkte (`login`, `register`, `refresh`, `logout`, beide
> Passwort-Reset-Routen, `verify-email`, `device-pairing/redeem`), die
> E-Mail-Wechsel-Bestätigung und die öffentliche KI-Sonde. Bleiben die vierzehn
> der beiden Tabellenzeilen. Fünf weitere — `POST /mcp`, `DELETE /mcp`,
> `POST /mcp/tools/{tool_name}`, `POST /mcp/rpc` und
> `POST /auth/service-accounts/validate` — tragen ihren eigenen
> API-Key-Authentifizierer und zählen je nach Lesart mit; dann heißt die
> Gesamtzahl 29 statt 24. An den vierzehn ändert das nichts, weil beide Lesarten
> dieselben zehn öffentlichen abziehen. Wer ohne diesen Filter nachmisst, bekommt
> 24, hält den Absatz für falsch und öffnet ihn erneut.
>
> Beide Router sind blanke `APIRouter(prefix=…)` ohne `dependencies=`, beide
> Elternrouter tragen ebenfalls keins (`tenant_scoped_router` führt nur
> `responses=AUTH_RESPONSES`, also Dokumentation), und `main.py` registriert keine
> Auth-Middleware. Der `{tenant_slug}` in der zweiten Zeile ist eine beliebige
> Zeichenkette.
>
> **Drei** der sieben lesen zusätzlich mandantenfremd aus der Datenbank:
> `mixing-protocol`, `mixing-safety` und `ec-budget` rufen
> `service.get_fertilizer(...)` (`router.py:69`, `:179`, `:270/287/293`), und
> `FertilizerService.get_fertilizer` überspringt die Prüfung bei `tenant_key=""`
> — dem Default, den alle drei benutzen. Die erste Fassung dieses Satzes nannte
> nur `mixing_protocol`; wer den Fix danach zuschneidet, härtet eine Route und
> lässt zwei formgleiche offen. Also genau die Teilreparatur, die dieser Absatz
> an sich selbst diagnostiziert.
>
> Dazu installationsweite Stammdaten-CRUD, die `Depends(get_current_user)` auf
> Router-Ebene trägt — also **Authentifizierung ohne jede Mandantenprüfung**,
> nicht „bloße Mitgliedschaft". Auf diesen Pfaden wird kein Mandant aufgelöst.
>
> **Dieser Absatz hat eine Fehlerhistorie, und die gehört hierher.** Sie steht
> als Liste und nicht als Zahl, weil die Zahl schon zweimal danebenlag — beim
> Zählen der eigenen Runden, in einem Absatz über das Nichtnachmessen:
>
> | Fassung | Was daran falsch war |
> |---|---|
> | #1395 (Original) | schrieb W-1 als „durch den zweiten Selektor aufgelöst" ab — gemessen deckt der Sweep die Fläche nicht ab |
> | #1407 | nannte `nutrient_calculations` als Quelle der Durchfaller — falsches Modul, es trägt null zu den 133 bei |
> | PR #1408, Runde 1 | nannte `calculations` und reduzierte die offene Fläche damit auf sieben — die Hälfte |
> | PR #1408, Runde 2 | Zahl richtig (vierzehn), Tabelle falsch: drei Routennamen waren Funktionsnamen (`flushing-protocol`, `runoff-analysis`, `water-mix-reverse`); ein Datenbank-Leser statt drei; und eine Korrektur an #1402 behauptet, die nie stattgefunden hatte |
> | PR #1408, Runde 3 | „vierzehn" ohne den Filter, der sie erzeugt (ungefiltert 24); und #1402s Titel für falsch erklärt, obwohl er korrekt ist |
> | PR #1408, Runde 4 | beschrieb den veralteten Verweis auf #1402, statt ihn zu reparieren |
> | PR #1408, Runde 5 | zählte die Kommentare an #1402 falsch durch; „alle sieben Operationen" für einen Router mit elf; `mcp ×3` als Pfadangabe |
> | PR #1408, Runde 6 | **diese Tabelle** war um eine Fassung verschoben: jede Zeile nannte den Commit, der den Fehler *behob*, statt den, der ihn enthielt |
>
> Dahinter steht ein Mechanismus, und Runde 6 hat ihn freigelegt: ich habe jedes
> Mal den benannten Teilaspekt repariert, statt die Aussage neu abzuleiten — und
> die Historie selbst aus dem Gedächtnis geschrieben statt aus dem Diff. Deshalb
> verschob sich die Tabelle genau um die Runde, die den jeweiligen Fehler behob.
> Die Zeilen oben stammen jetzt aus `git show` über jede einzelne Fassung.
>
> Inhaltlich hat alles denselben Ursprung: „unauthentifiziert" und „fällt durch
> beide Selektoren" sind zwei Eigenschaften, die sich auf verschiedene Module
> beziehen und nur bei einem zusammentreffen. In einem Nachtrag, dessen Gegenstand
> das Nichtnachmessen einer Zusage ist.
>
> Das steht als **#1402**. Dessen **Körper** nennt weiterhin nur die
> `nutrient_calculations`-Gruppe und bezeichnet sie als die Durchfaller — also die
> Verschmelzung, die dieser Absatz auflöst. Der **Titel** dagegen ist korrekt wie
> er dasteht: er nennt kein Modul, und „seven that require no authentication at
> all" trifft gemessen zu, denn genau sieben der 133 Durchfaller sind
> unauthentifiziert und nicht bauartbedingt öffentlich. Eine frühere Fassung
> dieses Satzes zog den Titel mit hinein — eine ungemessene Behauptung in dem
> Absatz, der ungemessene Behauptungen verhindern soll, und wer danach handelt,
> ändert einen richtigen Titel in einen falschen.
>
> Korrigiert ist dort **nur in Kommentaren**, und davon gibt es drei. Der zweite
> trug noch genau die Tabelle, die dieser PR repariert — dieselben drei
> Funktionsnamen, und `mixing_protocol` als einziger Datenbank-Leser —, sodass der
> Verweis auf die falsche Fassung zeigte. Der **jüngste** zieht sie nach, nennt den
> Filter hinter der Vierzehn und hält fest, dass der Titel korrekt ist; er ist die
> Fassung, mit der zu arbeiten ist. (Die Kommentare tragen Selbstüberschriften wie
> „Dritte Korrektur", die die Korrekturen der *Behauptung* zählen, nicht die
> Kommentare — eine frühere Fassung dieses Satzes hat die beiden Reihen verwechselt
> und einen vierten Kommentar erfunden.) Der **Körper** bleibt unverändert und ist
> Teil dieses Postens: wer #1402 bearbeitet, nimmt die Tabelle aus dem jüngsten
> Kommentar, nicht die aus Abschnitt A.
>
> Es ist der größte offene Posten aus Welle 1 und **in keine Welle eingeordnet**:
> §2 und §4 lassen ihn aus, und der Abhängigkeitsgraph in §1 führt ihn nicht — nur
> der W-1-Nachtrag dort verweist auf ihn, seit diesem PR. Das bleibt zu tun.
>
> **Unverändert gültig:** W-2 und die fünf extern blockierten Issues.
>
> **Nicht** die Wellenstruktur, §2 und §4: solange #1402 in keiner Welle steht,
> ist sie unvollständig, §4 *ist* die Abhängigkeitsordnung und lässt denselben
> Posten genauso aus, und der Abhängigkeitsgraph in §1 führt ihn ebenfalls nicht.
> Auch dieser Absatz war mehrfach falsch, auf dieselbe Weise und ohne
> Ordnungszahlen, weil die Tabelle oben die einzige Zählung dieses Blocks bleiben
> soll: er nannte die Wellenstruktur unverändert gültig; rettete danach die
> Abhängigkeitsordnung mit genau dem Argument, das sie widerlegt; übersah §1; und
> schrieb §1 dann pauschal ab, obwohl der W-1-Nachtrag dort inzwischen auf #1402
> verweist.


**24 offene Issues**, davon eines das Renovate-Dashboard (#12) → **23 Arbeitsposten**.
**2 offene PRs**: #1355 (Renovate, React 19.3.0) und #1348 (Draft, blockiert durch #1368).

Jede tragende Behauptung unten ist gegen `origin/develop` @ `d38e0232f` **gemessen**,
nicht aus dem Issue-Text übernommen. Drei Messungen kippen oder verschärfen die
Vorgabe des Issues, eine davon so weit, dass sie die Priorität des ganzen Plans
bestimmt.

---

## 0. Die drei Funde, die den Plan verändern

### F-1 — #1385 ist kein Konfigurations-Leseleck, sondern ein Token-Exfiltrationspfad

Das Issue beschreibt: jeder authentifizierte Nutzer kann die HA-Konfiguration lesen
und ersetzen. Gemessen ist der `/test`-Endpunkt der teurere Teil.

`src/backend/app/api/v1/admin/settings/router.py:121-146`:

```python
url   = body.ha_url            or effective["ha_url"]
token = body.ha_access_token   or effective["ha_access_token"]
...
validate_ha_url(str(url), allow_private=settings.ha_allow_private_endpoint)
resp = httpx.get(f"{url}/api/", headers={"Authorization": f"Bearer {token}"})
```

Ein Aufrufer liefert `ha_url` und **lässt `ha_access_token` weg**. Dann dialt der
Server die vom Aufrufer gewählte Adresse mit dem **echten Long-Lived-Token der
Installation** im `Authorization`-Header. `validate_ha_url` blockt link-local,
Metadaten und (ohne Opt-in) RFC1918 — eine **öffentliche, angreiferkontrollierte
Adresse passiert bestimmungsgemäß**, denn der Guard ist keine Allowlist.

Der Guard selbst benennt die Annahme, die hier nicht hält
(`common/url_safety.py:272-275`):

> an attacker who can set it (admin, or a **mis-scoped lower-privilege user**)
> could otherwise point it at `169.254.169.254` … to leak the token

Der SSRF-Guard wurde gegen genau diesen Fall gebaut und dann an eine Route
gehängt, die nur `get_current_user` verlangt. Das ist die Klasse
„Guard implementiert, Annahme darunter falsch".

**Folge:** #1385 ist der oberste Posten des Plans, nicht ein Aufräumposten.
Der Geschwister-Endpunkt `plant-identification/test` hat die Form **nicht** —
er dialt `settings.plantnet_base_url`, also eine feste Adresse; dort ist nur ein
Schlüssel-Orakel offen.

Gemessen zur Leseseite: `GET /admin/settings` maskiert den Token, liefert aber
`ha_url` samt Herkunft im Klartext.

> **Nachtrag: erledigt.** Der Router hat **elf** Operationen. Die vier
> Storage-Operationen trugen `Depends(require_platform_admin)` schon; die
> **sieben** übrigen hat #1385 nachgezogen, und das war die Lücke. Der zitierte
> Codeausschnitt steht nicht mehr bei `:121-146`, sondern bei `:151`. Der Exfiltrationspfad ist mit einem Positivtest belegt, der ihn
> *als Mechanismus* festhält, damit der Ablehnungstest daneben nicht still aufhört,
> etwas zu beweisen. Der Abschnitt bleibt stehen, weil er begründet, warum #1385
> oben steht — der Befund ist historisch, nicht offen.

### F-2 — Ein Docstring zertifiziert eine Ratenbegrenzung, die es nicht gibt

Aus der #1353-Triage, nicht aus dem Issue. `notifications/tenant_router.py:269`:

```python
async def send_test_notification(...):
    """Send a test notification through a specific channel.

    Rate limited to 5 requests per hour per user.
    """
```

Gegenprobe: `grep -c limiter` über die Datei ergibt **0**. Kein `@limiter.limit`,
keine Begrenzung im Service. Die Aussage im Docstring ist unbelegt.

> **Nachtrag: erledigt.** `notifications/tenant_router.py:10` importiert den
> `limiter`, `:271` trägt `@limiter.limit(settings.rate_limit_notification_test)`,
> und `tests/api/test_notification_test_rate_limit.py` fährt das Budget bis zur
> Ablehnung. Die Gegenprobe oben ist damit **falsch geworden** — sie steht hier als
> Befund, nicht als Zustand. Die Disposition am Ende des Abschnitts („entweder die
> Begrenzung bauen oder die Behauptung streichen") ist getroffen: gebaut.

Der Radius ist begrenzt und das gehört dazu: gesendet wird an den **eigenen**
konfigurierten Kanal des Aufrufers, mit festem Textkörper. Es ist kein
Spam-Vektor gegen Dritte, sondern unbegrenzter Selbstversand — Egress und Kosten,
nicht Missbrauch gegen Fremde. Die Disposition ist trotzdem binär: **entweder die
Begrenzung bauen, die der Docstring behauptet, oder die Behauptung streichen.**

### F-3 — Die ZAP-SQL-Injection ist ein Falschpositiv, und der Unterdrückungspfad kann sie nicht ausdrücken

#1376 und #1389 melden dieselbe Regel auf zwei Commits: `40018 — SQL Injection`,
`POST /api/v1/privacy/email-change/confirm`, Parameter `token`. Aus dem
Artefakt des Laufs 34528880950:

```
otherinfo = The page results were successfully manipulated using the boolean
            conditions [John Doe' AND '1'='1' -- ] and [John Doe' OR '1'='1' -- ]
            Data was NOT returned for the original parameter.
evidence  = (leer)
confidence= 2 (Medium)
```

Gemessener Pfad des Werts: `router.py:207` → `PrivacyService.confirm_email_change`
→ `self._token_engine.hash_token(raw_token)` → `EmailChangeRepository.get_by_token_hash`,
und dort:

```aql
FOR doc IN @@collection
  FILTER doc.verification_token_hash == @token_hash
```

Der Rohwert wird **gehasht**, bevor er die Abfrage erreicht, und die Abfrage ist
**bind-parametrisiert**. Der Angriffs-String kann als Syntax nirgends ankommen.
Alert 40018 ist hier die Boolean-Inferenz-Variante mit leerer Evidenz — ein
Falschpositiv.

**Aber:** eine pauschale Unterdrückung wäre falsch, und der vorhandene
Unterdrückungspfad kann nichts anderes. `tests/security/zap-api-rules.tsv` hat
das Format `<PluginID> <THRESHOLD> <Confidence> <Note>` — **keine URL-Spalte**.
Und SQL existiert in diesem System wirklich: `psycopg` gegen TimescaleDB,
`data_access/timescale/observation_repository.py`. Ein `40018 IGNORE` würde den
Scanner genau an der einzigen Stelle blind machen, an der SQL vorkommt.

**Folge für die Umsetzung:** die Unterdrückung gehört nicht in die tsv, sondern in
`scripts/security/zap_gate.py`, wo die §5.1/§5.2-Matrix ohnehin liegt — als
URI-gebundene Ausnahme mit geschriebenem Grund und Ablaufdatum.

**Und der eigentliche Defekt ist die Ausgabe, nicht der Fund:** der Post-Merge-Lauf
ruft `github.rest.issues.create` **ohne jede Deduplizierung**, mit dem Commit-SHA
im Titel. Ein dauerhafter Fund erzeugt damit unbegrenzt viele Issues. #1376 und
#1389 sind Exemplar 1 und 2 derselben Sache.

---

## 1. Abhängigkeiten und Widersprüche

### Abhängigkeitsgraph

```
#1352 (Anker über-ablehnend)  ──┐
                                ├──> ein geteilter Anker Location→Site→tenant_key
#1372 (Anker fehlt ganz)      ──┘        └──> #1349 (gemergt) zeigt die Form
                                         └──> MCP set_plant_location, Succession, Actuator

#1385 (admin/settings ungegated) ──> Welle 1
#1353 (Sweep-Guard)              ──> deckt #1385 NICHT ab (siehe W-1)

#1374 (Side-Services auf uv)  ──> #1383 AK „poetry darf nicht gelistet sein"
#1383 (uv selbstprüfend)

#1368 (WHC-Signal + Seeder-Upsert) ──> PR #1348 ──> #1175 ──> #1347 (extern blockiert)

Welle 1 (#1385, #1352, #1372, #1353) ──> #1361 (Release v0.4.0)   [Nutzerentscheidung]

#1061 (ADR-008)  ──> #779 (/api/v2, geparkt)
```

### W-1 — #1385 verweist auf einen Sweep, der es nicht erfassen wird

#1385 AK 3 lautet: „The router is covered by the repository-wide write-route sweep
from #1353 once it exists."

#1353 spezifiziert den Guard aber so: „selects every route under `/t/{tenant_slug}`
with a non-GET method". `/api/v1/admin/settings` liegt **nicht** unter diesem
Präfix. Die Abdeckung, auf die #1385 sich stützt, entsteht so nicht.

> **Nachtrag: die Auflösung im nächsten Absatz ist durch Messung widerlegt.** Zwei Selektoren
> decken **nicht** jede schreibende Route ab — 133 fallen durch beide, und
> **vierzehn** nicht bauartbedingt öffentliche Schreibrouten sind ohne jede
> Authentifizierung erreichbar (sieben davon unter den Durchfallern, sieben
> weitere sieht der Selektor und meldet sie nicht; den Filter hinter der Zahl
> führt der Nachtrag am Kopf dieses Dokuments). AK 3 von #1385 ist damit *nicht* auf diesem
> Weg erfüllt; siehe den Nachtrag am Kopf dieses Dokuments und **#1402**. Der
> Absatz bleibt stehen, weil er die Entscheidung festhält, die getroffen wurde.

**Auflösung, im Plan verbindlich:** Der Guard aus #1353 wird auf **jede
schreibende Route mit einem nicht-öffentlichen Gate** ausgeweitet, mit zwei
Selektoren statt einem — tenant-scoped (`/t/{slug}`, Kontext über `ctx`) und
installationsweit (`/admin/**`, Kontext über `_current_user`). AK 3 von #1385
bleibt dann gültig. Der Alternativweg wäre, AK 3 aus #1385 zu streichen; das
verlöre die einzige mechanische Absicherung gegen die nächste Drift in diesem
Router.

### W-2 — #1383s Abnahmebedingung ist erst nach #1374 erfüllbar

#1383 fordert: „`poetry` must not [be listed]". Gemessen im Dashboard #12 heute:

| Manager | Dateien |
|---|---|
| `pep621` | `src/backend/pyproject.toml` ✓ |
| `poetry` | `src/inference-service/pyproject.toml`, `src/knowledge-service/pyproject.toml` |
| `pip_requirements` | `docker/embedding-service/`, `docker/reranker-service/`, `docs/`, `src/inference-service/` |

Die `poetry`-Einträge sind heute **korrekt** — es sind genau die Side-Services,
die #1374 nach uv holen soll. Eine Health-Lane, die diese Bedingung jetzt prüft,
wäre am Tag ihrer Entstehung rot. **#1374 vor #1383**, oder #1383s Inventar-Assertion
beschreibt den Übergangszustand explizit mit.

### W-3 — #1383s Auslöser existiert derzeit nicht

Die Begründung für die Renovate-Health-Lane sind sechs stumme Wochen mit einem
`⚠️ WARN: pip-compile error` im Dashboard. Gemessen: das Dashboard #12 hat **heute
keinen Abschnitt `Repository Problems`** — die Warnung ist mit #1377 verschwunden,
weil der `pip-compile`-Manager weg ist.

Das entwertet die Lane nicht, ändert aber ihren Nachweis: sie kann **nicht** gegen
Livedaten falsifiziert werden. Der Beweis muss über einen injizierten
Dashboard-Körper laufen — was #1383s AK bereits so formuliert. Hier nur festgehalten,
damit niemand vergeblich nach dem echten WARN sucht.

### W-4 — #1361 wartet jetzt bewusst auf Welle 1

**Nutzerentscheidung 2026-09-11: erst nach den Sicherheitsfixes veröffentlichen.**

Damit ist Welle 1 der kritische Pfad für die Auslieferung, und die Lag-Meldung
läuft bis dahin weiter (sie kommentiert täglich). Zwei Dinge, die im Issue falsch
oder veraltet sind und beim Ausführen auffallen würden:

- Der Draft heißt inzwischen **`v0.4.0`**, nicht `v0.3.1` wie im Issue-Körper.
- Die neueste veröffentlichte Version ist `v0.3.0` vom 2026-08-31.

Gemessen und für die Entscheidung relevant: #1385 und #1372 sind **vorbestehend**,
laufen also bereits in der produktiven `v0.3.0`. Das Warten senkt die Exposition
nicht, es bündelt nur die Behebung in eine Auslieferung.

### W-5 — #1347 ist keine Ingenieursaufgabe

#1347 verlangt eine zitierte, massenbezogene CEC-Zahl je Substratmaterial mit
benannter Messmethode (BaCl₂, NH₄OAc pH 7 oder die gartenbauliche pH-4-Variante)
plus die Schüttdichte derselben Quelle. Das ist Literaturrecherche, kein Code.
Es kann aus einer Sitzung heraus nicht abgeschlossen werden und ist im Plan als
**extern blockiert** geführt, nicht als Arbeitspaket.

---

## 2. Wellen

Die Wellen sind nach Abhängigkeit geschnitten, nicht nach Thema. Innerhalb einer
Welle sind die Pakete parallelisierbar, sofern sie keine Datei teilen.

### Welle 1 — Sicherheitskern (kritischer Pfad, blockiert das Release)

| Paket | Issue | Umfang | Parallel? |
|---|---|---|---|
| **P1.1** | #1385 | `require_platform_admin` auf allen 7 ungegateten `/admin/settings`-Operationen | ja |
| **P1.2** | #1352 + #1372 | Ein geteilter Anker Location→Site→`tenant_key`, beide Richtungen | ja |
| **P1.3** | #1353 | Mechanischer Schreibrouten-Guard + Triage der 37 | nach P1.1/P1.2 |

#### P1.1 — #1385, die sieben ungegateten Operationen

Gemessen, `admin/settings/router.py`:

| Route | heute | Ziel |
|---|---|---|
| `GET /admin/settings` | `get_current_user` | `require_platform_admin` |
| `PUT /admin/settings/home-assistant` | `get_current_user` | `require_platform_admin` |
| `POST /admin/settings/home-assistant/test` | `get_current_user` | `require_platform_admin` |
| `DELETE /admin/settings/home-assistant` | `get_current_user` | `require_platform_admin` |
| `PUT /admin/settings/plant-identification` | `get_current_user` | `require_platform_admin` |
| `POST /admin/settings/plant-identification/test` | `get_current_user` | `require_platform_admin` |
| `DELETE /admin/settings/plant-identification` | `get_current_user` | `require_platform_admin` |
| `*/storage` (4 Routen) | `require_platform_admin` ✓ | unverändert |

**Die offene Frage des Issues ist durch Messung entschieden.** #1385 stellt
`require_platform_admin` gegen `require_admin_scope("technical")` zur Wahl.
`require_admin_scope` löst über `Depends(get_current_tenant)` auf (`common/auth.py:532`)
— es ist mandantengebunden. `/api/v1/admin/settings` trägt keinen Mandanten im
Pfad und schreibt installationsweite Einstellungen. `require_admin_scope` wäre dort
nur mit einer erfundenen Mandantenauflösung anwendbar. **`require_platform_admin`**,
wie die Geschwister unter `admin/platform`.

*Falsifizierung, verbindlich:* ein Test, der **gegen den heutigen Router rot** ist —
ein einfaches Mitglied erhält 403 auf jede der sieben Routen. Und getrennt davon
der Exfiltrationsfall aus F-1 als eigener Test: `POST …/home-assistant/test` mit
fremder `ha_url` und **ohne** `ha_access_token` erreicht den `httpx.get` nicht.

#### P1.2 — #1352 + #1372, ein Anker für beide Fehlerrichtungen

Die beiden Issues sind dieselbe fehlende Invariante in entgegengesetzter Richtung:

- **#1352, über-ablehnend:** `SiteService.get_location/get_slot` vergleichen den
  Mandanten des Aufrufers mit dem **leeren** `tenant_key` des Location-Dokuments
  und lehnen damit die **eigenen** Standorte ab. Drei Aufrufer betroffen, darunter
  MCP `set_plant_location`, das damit jede legitime Verschiebung mit 404 beantwortet.
- **#1372, unter-ablehnend:** `PlantingRunService.create_run` und `update_run`
  prüfen `location_key` überhaupt nicht. Gemessen: `location_key` steht in
  `allowed_fields` (`planting_run_service.py:163`), und
  `_get_available_slots` → `get_slots_by_location` (`site_repository.py:235`)
  ist eine ungefilterte `HAS_SLOT`-Traversierung. Die Materialisierung schreibt
  dann Pflanzen, `PLACED_IN`-Kanten und `currently_occupied` in die Slots eines
  fremden Mandanten.

**Die richtige Form existiert bereits.** PR #1349 hat `_resolve_placement` in
`PlantInstanceService` gebaut — eine Kettenauflösung `site_key → location_key →
slot_key`, verankert an der Site, mit der dort dokumentierten Begründung, warum
Feld-für-Feld-Prüfung nicht genügt (drei eigene Objekte beweisen keinen Ort).

Das Paket hebt diesen Anker nach `SiteService`, und **alle fünf** Aufrufer nutzen
ihn: `PlantInstanceService._resolve_references`, `SiteService.get_location/get_slot`,
MCP `SetPlantLocation`, `SuccessionPlanService`, `ActuatorService` — plus der neue
in `PlantingRunService`. Eine sechste private Kopie wäre genau der #948-Rückfall.

*Falsifizierung, verbindlich, beide Richtungen:*
- **Positiv (heute rot):** eine Location unter der eigenen Site, mit dem eigenen
  Mandantenschlüssel gelesen, wird gefunden.
- **Negativ:** eine Location unter fremder Site liefert weiterhin `NotFoundError`.
- **Kontrolle:** ein Durchlauf mit legitimer eigener Location materialisiert
  weiterhin. Ohne diese Kontrolle besteht ein Guard, der **alles** ablehnt, die
  ersten beiden — das ist der #706-Fehler, den #1352 gerade dokumentiert.
- **Kein Schreibzugriff:** ein Test, der beweist, dass bei fremder Location **keine**
  `PLACED_IN`-Kante und **kein** `update_slot` den fremden Mandanten erreicht.

*Offen, muss im Paket entschieden und aufgeschrieben werden:* Was geschieht mit
Durchläufen, die **heute schon** einen fremden oder toten `location_key` tragen?
Der Guard allein fasst sie wegen der changed-only-Semantik nicht an. Empfehlung:
bei der nächsten Materialisierung ablehnen, kein Reparaturlauf — und diese
Entscheidung in den Docstring, nicht nur in den PR.

#### P1.3 — #1353, der mechanische Guard

Die Zahl aus dem Issue ist **nachgemessen und trifft zu**, mit einer Verschiebung:

| | Issue | gemessen @ `d38e0232f` |
|---|---|---|
| Schreibrouten in 43 `tenant_router.py` | 243 | **249** |
| davon `ctx` über blankes `get_current_tenant` | 37 | **37** |

Die Gesamtzahl wandert (PR #1349 hat Routen ergänzt) — der Guard muss sie also
**berechnen**, nie als Literal führen.

Die Triage der 37 ist unten vollständig vorgeschlagen, damit das Paket nicht mit
einer Analyse beginnt, die schon getan ist. Jede Zeile bleibt vom Umsetzenden zu
prüfen; die drei mit † markierten sind die, bei denen die Zuordnung ausschließlich
auf einer Aussage im Docstring beruht — und F-2 zeigt, was solche Aussagen wert
sein können.

**Allowlist-Kandidaten, per-Nutzer-Zustand (17):**
`favorites` add/remove · `ki_assistent` refresh_tips, dismiss_tip, acted_on_tip,
dismiss_daily_tip · `notifications` mark_read, mark_acted, update_preferences,
subscribe_pwa, unsubscribe_pwa, **send_test_notification †** ·
`onboarding` complete, skip, reset, update_progress · `user_preferences` update

**Allowlist-Kandidaten, POST-als-Berechnung (5):**
`nutrient_plans` calculate_dosages · `tanks` calculate_ec_dilution ·
`plant_instances` validate_planting · `tasks` validate_hst ·
**`actuators` test_rule †** (Docstring: „without side effects")

**Gate erforderlich (15):**

| Route | Grund | Gate |
|---|---|---|
| `diagnose` analyze | Cloud-Egress mit `photo_ref` + Symptomen | `require_tenant_role(GROWER)` |
| `ki_assistent` explain, create_conversation, send_message | LLM-Egress + Kosten | `require_tenant_role(GROWER)` |
| `ki_assistent` delete_conversation | Schreibzugriff | `require_tenant_role(GROWER)` |
| `tasks` start, complete, skip, reopen | Domänen-Schreibzugriff | `require_permission(TASK, UPDATE)` |
| `watering_events` confirm, quick_confirm | Domänen-Schreibzugriff | `require_permission('watering-event', CREATE)` |
| `watering_logs` confirm, quick_confirm | Domänen-Schreibzugriff | `require_permission('watering-log', UPDATE)` |
| `planting_runs` request/cancel_run_diary_entry_analysis | Regel liegt laut #1353 im Service — **nachmessen**, nicht glauben | offen |

Die `ki_assistent`-Gruppe steht nicht im Issue und ist das genaue Argument, das
#1341 für `diagnose/analyze` gemacht hat: eine Rolle, die keine Domänendaten
anlegen darf, soll keine Daten aus der Installation herausgeben — und ein Viewer
könnte es in einer Schleife tun.

*Zwei getrennte Ergebnisse, damit keines das andere verdeckt:*
1. Der Guard selbst — ein Unit-Test, der über `app.routes` läuft und dabei
   `_IncludedRouter.original_router` auflöst (`include_router` legt **nicht** flach,
   siehe `test_harvest_indicator_admin_mount.py`), **zwei** Selektoren führt
   (tenant-scoped und `/admin/**`, siehe W-1) und jeden Allowlist-Eintrag mit
   geschriebenem Grund verlangt. Ein veralteter Eintrag muss rot werden, so wie
   `check_layer_imports` es bereits tut.
2. Die 37 Dispositionen als Code.

*Falsifizierung:* der Guard ist **gegen heutiges `develop` rot** (37 Funde) und
grün nach der Triage. Und eine zweite Gegenprobe, die die erste nicht leistet:
eine absichtlich wieder ungegatete Route wird rot, und ein Allowlist-Eintrag für
eine gelöschte Route ebenfalls.

### Welle 2 — Auslieferung (startet, sobald Welle 1 gemergt ist)

| Paket | Issue | Umfang |
|---|---|---|
| **P2.1** | #1361 | `release-publish.yml` mit Tag `v0.4.0` auslösen, danach Lag-Lauf zur Bestätigung |
| **P2.2** | #1236 | Hop 4 messen: den unversionierten Entwurf prüfen und übernehmen |

**P2.1** ist ein Kommando, kein Bau: `gh workflow run release-publish.yml -f tag=v0.4.0`.
Vorher der Gate-Durchlauf des `release-publish-trigger`-Skills, das die
Vorbedingungen prüft (genau ein offener Draft, Tag vom develop-Tip erreichbar,
Pflicht-Checks grün). Danach kommentiert der nächste geplante Lag-Lauf das Issue
selbst zu.

**P2.2** übernimmt drei unversionierte Dateien, die #1236 in einem Harness-Worktree
gefunden hat (286 + 850 + 674 Zeilen). Vor der Übernahme drei Prüfungen, die das
Issue selbst benennt und die nicht übersprungen werden dürfen: die Anker-/Ordnungsfrage
(misst der Check einen Zustand, der älter ist als die Behauptung?), ob CI eine
private Instanz überhaupt erreichen soll, und ob `HEALTH_EXPOSE_BUILD_REVISION`
als Opt-in den Check in genau der Umgebung inert macht, für die er gedacht ist.
Der letzte Punkt ist die #1218-Falle; die Antwort gehört in den Workflow-Kopf.

### Welle 3 — Scanner-Hygiene (unabhängig, jederzeit)

| Paket | Issue | Umfang |
|---|---|---|
| **P3.1** | #1376 + #1389 | Falschpositiv URI-gebunden im Gate ausnehmen **und** die Issue-Erzeugung deduplizieren |

Zwei Hälften, die zusammengehören, weil die eine ohne die andere den Rückstand
weiter wachsen lässt:

1. **Ausnahme im Gate, nicht in der tsv** (Begründung: F-3). In
   `scripts/security/zap_gate.py` eine Ausnahmeliste aus `(pluginid, uri-Muster,
   Grund, Ablaufdatum)`. Ablauf abgelaufen → das Gate wird laut, nicht still.
   Die tsv bleibt leer; ein `40018 IGNORE` dort würde TimescaleDB ungeschützt lassen.
2. **Deduplizierung der Issue-Erzeugung.** Heute: `github.rest.issues.create`
   ohne Suche, Titel mit Commit-SHA. Ziel: ein offenes Issue pro **Fund-Identität**
   (pluginid + URI), das bei erneutem Auftreten kommentiert statt neu angelegt wird
   — die Form, die `release-lag.yml` bereits hat und die man dort abschauen kann.

*Falsifizierung:* der Gate-Lauf über das gespeicherte Artefakt von 34528880950
ist heute rot und nach der Ausnahme grün; **und** derselbe Lauf mit auf `/api/v1/tasks`
geänderter URI bleibt rot. Ohne die zweite Probe ist die Ausnahme unbegrenzt.

*Messnotiz zur Laufhistorie, damit sie nicht falsch gelesen wird:* von den letzten
30 Läufen sind 18 `skipped`. Das ist **kein** Defekt — die `pull_request:
types: [labeled]`-Lane feuert bei **jedem** Label auf jedem PR, und die Auswahl
`security-scan` findet erst im Job-`if:` statt. Der Lauf meldet dann ehrlich
`skipped`. Es macht die Historie nur schwer lesbar.

*Nicht Teil des Pakets, aber im Artefakt gesehen und notiert:* neun `5xx`-Antworten
(Low) im API-Scan. Fuzzing, das 500er erzeugt, ist ein eigener Posten; hier nur
festgehalten, damit er nicht mit diesem Fund verschmilzt.

### Welle 4 — Toolchain (Reihenfolge ist bindend)

| Paket | Issue | Umfang | Reihenfolge |
|---|---|---|---|
| **P4.1** | #1374 | Vier Side-Services auf `pyproject.toml` + `uv.lock`, `uv sync --locked` im Dockerfile | zuerst |
| **P4.2** | #1383 | Sechs strukturelle Nachbesserungen zu #1377 | nach P4.1 (W-2) |
| **P4.3** | #1375 | `.resume/<slug>/plan.md`-Stub in `scripts/worktree_add.sh` säen | jederzeit |

P4.2 zerfällt in sechs unabhängige Teile, die einzeln lieferbar sind. Der
wertvollste ist Teil 2, der **dauerhafte Hash-Falsifizierer** — er sichert die
Aussage des Gates, nicht seine Verdrahtung. #1377 hat gemessen, dass
`uv lock --check` **keine** Hashes prüft und der erste Manipulationstest vakuös
war (sdist-Hash statt des tatsächlich installierten Wheels). Ohne diesen Test hat
die Behauptung „Installationen sind hash-verifiziert" keinen Widerleger.

P4.3 ist klein und hat eine Falle, die das Issue bereits benennt: ein erneuter
Lauf gegen einen bestehenden Worktree darf einen **gefüllten** Plan nicht
überschreiben.

### Welle 5 — Substratkatalog (eine Kette, ein PR hängt daran)

| Paket | Issue | Umfang |
|---|---|---|
| **P5.1** | #1368 | Zwei Entscheidungen + Umsetzung; entsperrt PR #1348 |
| **P5.2** | #1175 | PR #1348 mergen, Reststand des Issues fortschreiben |
| — | #1347 | **extern blockiert**, siehe W-5 |

**P5.1, Teil 1 — welches Signal der Retention-Modifikator liest.**

**Nutzerentscheidung 2026-09-11: das Enum entscheidet; die Zahl bleibt Anzeige- und
Mischungsgröße.**

Gemessen, `watering_volume_engine.py:277-284`:

```python
if water_holding_capacity_percent is not None:
    whc_modifier = 1.0 + (50.0 - water_holding_capacity_percent) / 100.0
    whc_modifier = max(0.5, min(1.5, whc_modifier))
```

WHC 80 → ×0,70. WHC 28 (der gequellte Container-Kapazitätswert bei pF 1, EN 13041)
→ ×1,22. Also +74 % Wasser pro Gabe, und in der Gegenrichtung zum Enum, das
`high` sagt (→ ×0,80).

Gemessen zum Umfang der Entscheidung: das Feld wird **nicht nur** vom Gießpfad
gelesen. `substrate_mix_engine.py:182` aggregiert es gewichtet für Mischungen, und
`mcp_server/tools/substrates.py:64` gibt es aus. Es kann also nicht entfallen — nur
der Gießmodifikator hört auf, es zu lesen.

*Nachgemessen statt offengelassen* (die erste Fassung dieses Absatzes stellte es
als offene Frage, obwohl die zwei Zeilen vorher zitierte Datei sie beantwortet):
eine berechnete Mischung **trägt** ein `water_retention`-Enum.
`substrate_mix_engine.py:179` gewichtet `_RETENTION_ORDER[s.water_retention]`, und
Zeile 219 gibt `"water_retention": _resolve_retention(retention_num)` zurück. Der
Gießpfad verliert für Mischungen also nichts, wenn der `if`-Zweig fällt.

*Zusätzlich, aus dem Issue:* ein Katalogtest Enum ↔ Band. Heute prüft nichts, dass
`water_retention: high` und `water_holding_capacity_percent: 28` einander
widersprechen. REQ-019 Zeile 50 nennt die Bänder (low < 30 %).

**P5.1, Teil 2 — der Seeder aktualisiert einen bestehenden Katalog nie.**
`seed_substrates.py:47` überspringt jeden Datensatz, dessen `(type, name_de)` schon
existiert. Jede vor einer Korrektur aufgesetzte Installation behält die alten
Zahlen auf Lebenszeit, während die Testsuite die YAML als korrigiert zertifiziert.
Dieselbe Lücke hatte #1332 für Steinwolle/PON-EC.

*Empfehlung:* eine Migration, die korrigierte Katalogwerte auf **unveränderte**
Seed-Datensätze neu anwendet. Ein vom Mandanten bearbeiteter Datensatz darf nicht
überschrieben werden — das ist die eigentliche Schwierigkeit und der Grund, warum
ein bloßer Upsert nicht genügt.

*Aus dem Review von #1368, klein aber echt:* die Gaudig-et-al.-Zitation lebte nur
im Test-Literal, das PR #1348 löscht; REQ-019 nennt `pon_mineral` weiterhin
„pH-neutral (6.0–7.0)", während der einzige PON-Datensatz 8,2 trägt; und die
pH-Konvention steht dreimal ausgeschrieben, wo der CEC-Präzedenzfall (#1152 §F)
eine Fundstelle plus Zeiger vorsieht.

### Welle 6 — Frontend und Qualität (unabhängig, parallel)

| Paket | Issue | Umfang |
|---|---|---|
| **P6.1** | #1390 | `AdminEditTenantPage` + `AdminEditUserPage`: 403 / 5xx / Netzfehler vom „nicht gefunden" trennen |
| **P6.2** | #1373 | Dashboard: die zwei selbstladenden Widgets in das eine Ladesignal einhängen |
| **P6.3** | #1393 | Task-Fotos: Janitor + DELETE-Route + Löschkaskade |

**P6.1** — gemessen, `AdminEditTenantPage.tsx:85-95`: `fetchAdminTenants()` gefolgt
von `.finally`, **kein** `.catch`. Jede Ablehnung lässt `tenant` auf `null`, und
Zeile 193 rendert dann `pages.admin.tenantNotFound`. Die Ablehnung ist zusätzlich
unbehandelt. `AdminEditUserPage` ist formgleich — **beide zusammen**, sonst driftet
das Geschwister (#948).

Die zweite Ursache steht im Issue und ist die interessantere: beide Seiten laden
**alle** Mandanten bzw. Nutzer und wählen clientseitig aus. „Nicht in der Liste"
und „die Anfrage ist fehlgeschlagen" kommen deshalb als dasselbe `null` an. Ein
`.catch` allein trennt die drei Antworten nicht vollständig; entweder kommt ein
Lesezugriff per Schlüssel dazu, oder die Unterscheidung hängt an der Ablehnung.
Das ist die Entscheidung des Pakets.

**P6.2** — das Issue empfiehlt Option 1 (Registrierung in das Seitensignal), und
die Empfehlung trägt: sie hält die Invariante „genau ein Lade-Ansager auf dem
Dashboard" und deckt jedes künftige selbstladende Widget mit ab.

*Abnahme mit Messbedingung, die nicht überlesen werden darf:* der Nachweis muss
**mitten im Laden** erfolgen, mit offen gehaltener langsamer Anfrage. Ein Scan auf
der fertig geladenen Seite kann diesen Defekt grundsätzlich nicht sehen — dieselbe
Bedingung, an der #1337 hing.

**P6.3** — Nutzerentscheidung 2026-09-11: Janitor **und** DELETE-Route **und**
Löschkaskade, `add_photo_ref` entfernen. Vier Teile:

1. Ein Celery-Janitor (NFR-011-Nachbarschaft) entfernt `TASK`-Anhänge älter als N
   Stunden, die kein Task referenziert. Deckt alle drei gemessenen Lecks.
2. `DELETE /tasks/{key}/photos/{attachment_id}` mit dem Attachment-Gate, nach dem
   Vorbild der Pflanzengalerie — damit der Entfernen-Knopf tut, wonach er aussieht.
   Heute filtert `PhotoUpload.handleRemove` nur lokalen Zustand.
3. `TaskService.delete_task` löscht die Anhänge mit. Das ist unumkehrbar und
   deshalb explizit aufzuschreiben; abgesichert dadurch, dass ein Task nur in
   `pending`/`skipped`/`cancelled`/`dormant` löschbar ist — die Dokumentation eines
   abgeschlossenen Tasks geht so nie verloren.
4. `TaskService.add_photo_ref` **entfernen**. Gemessen: genau ein Aufrufer im
   ganzen Repo, und der ist ein Fail-Closed-Test
   (`test_task_service_fail_closed.py:87`). Die Methode hängt an `photo_refs` an,
   ohne die Referenz gegen den Anhangkatalog aufzulösen — sie würde beim
   Verdrahten genau das Loch wieder öffnen, das #1388 geschlossen hat.

*Abnahme:* eine Quota-Messung vor und nach dem Janitor-Lauf zeigt, dass die Regel
greift. Zählen allein genügt nicht — `_enforce_quota` zählt **jede** Anhangzeile
des Mandanten (`attachment_service.py:210`), verlinkt oder nicht.

### Welle 7 — ADR-008 (groß, eigener Strang)

**Nutzerentscheidung 2026-09-11: #1061 ja, #779 geparkt.**

| Paket | Umfang |
|---|---|
| **P7.0** | Phase 0 — Inventar mit Beweis; die Zahl ist die Ratschen-Basis, **berechnet**, nicht versioniert (NFR-018 §2.1) |
| **P7.1** | Phase 1 — jeder Scheduler über `RecurrenceEngine`; die Care-Engine bleibt Intervall-Autorität und drückt das Ergebnis als `RRULE` aus |
| **P7.2** | Phase 2 — jeder Notification-Schreibzugriff über `NotificationPropagationService` |
| **P7.3** | Phase 3 — eine gemeinsame Abschlusstransition für done/skipped/rescheduled |
| **P7.4** | Phase 4 — die zwei Regeln in `BACKEND.md` und, wo mechanisch prüfbar, in ein Gate |

Jede Phase ist für sich lieferbar. Phase 0 liefert die Zahl, gegen die die Phasen
1–3 messbar sind — ohne sie ist „konsolidiert" eine Behauptung. Phase 4 ist nicht
optional: ohne sie ist das Ergebnis eine Momentaufnahme, keine Invariante, und die
nächste Kante baut die Duplizierung nach.

Vorarbeit liegt in `.audits/adr-008-phase-0-inventory/`.

**#779 bleibt offen** mit zwei aufgeschriebenen Freigabebedingungen:

1. `spec/project/rest-api-design/` ist auf `nolte/claude-shared@develop` gemergt.
2. #1061 ist mindestens durch Phase 3.

Die zweite ist die eigentliche: eine `/api/v2`-Migration **vor** der Konsolidierung
portiert drei duplizierte Subsysteme nach v2 und bezahlt die Konsolidierung danach
ein zweites Mal, auf zwei Oberflächen. Das gehört als Kommentar an #779, damit die
Reihenfolge nicht als Vergesslichkeit gelesen wird.

---

## 3. Extern oder auf ein Ereignis blockiert

Diese fünf sind **keine** Arbeitspakete. Sie stehen hier, damit niemand sie erneut
als offene Arbeit aufgreift.

| Issue | Blockiert auf | Freigabebedingung |
|---|---|---|
| #1292 | ein Ereignis | Ein `e2e-nightly`-Lauf, dessen `create_care_task` fehlschlägt. Die selbstdiagnostizierende Fassung (#1288) liegt auf `develop` und benennt dann den Zweig. AK 1 ist durch nichts erfüllbar, was hier getan wird. **Den 15-s-Puffer nicht erhöhen** — begründet im Issue, Präzedenz #1204. |
| #1321 | extern, zweiteilig | `nolte/gh-plumbing#417` ist **weiterhin offen** (heute geprüft), **und** es braucht ein Docker-Hub-Konto samt Token. Bis dahin ändert sich hier nichts; der Aufruf ohne `with:`/`secrets:` bleibt gültig. |
| #1223 | Beweis | Eine beobachtete `changes`-Job-Konklusion `failure` oder `cancelled` auf einem develop-Push. Bewusst **nicht** gebaut; die Begründung steht im Issue und ist gemessen. |
| #1347 | Literatur | Zitierte massenbezogene CEC-Zahl je Material mit benannter Methode und der Schüttdichte derselben Quelle. Siehe W-5. |
| #618 | Zugangsdaten | `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` (Workers-AI-Scope). Der Branch `feat/kami-media-pipeline` ist gebaut und trocken validiert, aber nie gepusht. |

Für #618 gibt es **einen** Teil, der ohne Zugangsdaten geht: den Branch als PR
öffnen, damit die 79 Render-Jobs und der Reviewer-Agent nicht in einem
unversionierten Worktree liegen. Das ist die Lehre aus dem CLAUDE.md-Abschnitt zur
Absturzwiederherstellung und aus #1236, wo genau so drei fertige Dateien fast
verloren gingen.

---

## 4. Reihenfolge und Parallelität

```
Tag 1      W1: P1.1 (#1385)  ║  P1.2 (#1352+#1372)        ← beide sofort, verschiedene Dateien
           W3: P3.1 (ZAP)    ║  W4: P4.3 (#1375)
           W6: P6.1 (#1390)  ║  P6.2 (#1373)

Tag 2      W1: P1.3 (#1353)                                ← braucht P1.1/P1.2 als Vorlage
           W4: P4.1 (#1374)
           W5: P5.1 (#1368)
           W6: P6.3 (#1393)

Tag 3      W2: P2.1 (#1361 Release v0.4.0)                 ← Freigabe: W1 gemergt
           W4: P4.2 (#1383, sechs Teile)                   ← Freigabe: P4.1 gemergt
           W5: P5.2 (PR #1348 mergen)

danach     W2: P2.2 (#1236)
           W7: P7.0 … P7.4 (ADR-008, eigener Strang)
```

**Zwei Kollisionen, die den Merge-Train sonst blockieren:**

- P1.2 und P1.3 fassen beide `tenant_router.py`-Dateien an. P1.2 zuerst mergen,
  dann P1.3 rebasen — `strict: true` auf `develop` erzwingt das ohnehin, und der
  Train trägt erfahrungsgemäß maximal etwa drei PRs.
- P5.1 und P5.2 sind dieselbe Datei (`substrates.yaml`) und derselbe PR-Strang.
  Strikt sequenziell.

`develop` verlangt genau zwei Pflicht-Checks: `static / Static CI Tests` und
`lint-test-build (22)`. Der Präfix zählt — ein Merge-Kommando, das nur
`Static CI Tests` nennt, findet den Check nicht.

---

## 5. Methodische Auflagen für jedes Paket

Diese stehen hier, weil sie in diesem Repository jeweils schon einmal Geld
gekostet haben. Sie sind Teil der Abnahme, nicht Stilfragen.

1. **Die behauptete Ursache nachmessen, nicht nur den behaupteten Defekt.** In den
   Sweeps vom 15. und 22. August war je dreimal die Diagnose im Issue falsch — der
   vorgeschlagene Fix wäre inert gewesen. In diesem Plan hat es F-1, F-2 und F-3
   bereits dreimal getroffen.
2. **Der Falsifizierungstest muss denselben Ausdruck prüfen wie die Regel.** Prüft
   er eine benachbarte Aussage, ist er grün, während die Regel inert ist.
3. **Die Gegenprobe nie per `git stash`.** `git stash push`/`pop` legt die Datei
   **unstaged** zurück; PR #1341 wurde so mit nur der Testdatei gepusht, ohne den
   Sicherheitsfix. `cp` statt `stash`, und `git show --stat HEAD` vor jedem Push.
4. **Ein Positivtest braucht eine Eingabe, die das Echte ablehnt und das Double
   annimmt.** Erfindet die Fixture eine unmögliche Form, zertifiziert der grüne
   Test nichts (#947, #1155).
5. **Kontrolle gegen den über-ablehnenden Guard.** Ein Guard, der alles ablehnt,
   besteht jeden Negativtest. Gilt hier besonders für P1.2 — #1352 ist genau
   dieser Fehler.
6. **Zahlen berechnen, nie als Literal führen.** Die 249 Schreibrouten wandern mit
   jedem PR.
7. **Lange Arbeit als Top-Level-Sitzung in einem Worktree unter der zentralen
   Worktree-Wurzel**, nie aus `.claude/worktrees/`. Der `guard-nested-worktree`-Hook
   lehnt Commits von dort ab.

---

## 6. Was dieser Plan bewusst nicht enthält

- **Eine Gegenprüfung für #1223.** Die Zahl stammt aus dem Issue, nicht aus einer
  eigenen Messung: über 60 Läufe schloss der `changes`-**Job** 58× `success`,
  2× `skipped`, **0×** `failure`. Was hier nachgemessen ist, liegt eine Ebene höher
  und widerspricht dem nicht: von den letzten 60 `docker-publish`-**Läufen** waren
  59 grün und einer rot. Die Kosten der Gegenprüfung fallen auf
  jedem Push an, ihr Falschpositiv rötet die Lieferbahn für alle. Ein
  Laufzeit-Guard für einen unbeobachteten Fehler, dessen falsche Antwort schlimmer
  ist als der Fehler, ist spekulatives Gerüst.
- **Eine Erhöhung des E2E-Puffers in #1292.** Begründet oben.
- **Ein CEC-Band aus den heutigen Werten (#1347).** Es würde jede Zeile bestehen
  und damit genau die Werte ratifizieren, die zur Debatte stehen.
- **#779.** Geparkt mit aufgeschriebener Freigabebedingung, siehe Welle 7.
