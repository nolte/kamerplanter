---
artifact-type: issue-orchestration-analysis
repo: nolte/kamerplanter
issue: 566
classification: docs
secondary-classes: [refactor]
route: direct
status: in_progress
created: 2026-07-12
---

# Issue Orchestration — Pre-analysis

## Issue metadata

- **Repository**: nolte/kamerplanter
- **Issue**: #566 — Analysis: consolidate Python backend dependencies to a single Renovate-updated source (pyproject.toml vs requirements.txt)
- **URL**: https://github.com/nolte/kamerplanter/issues/566
- **Labels**: chore, dependencies, backend
- **Linked items**: none (PR #563 is the *triggering* incident, not a fix for this issue)
- **Prior art checked**: no `project/features/` entry, no `project/roadmap.md` item, no open PR. PR #563 (`chore(deps): recompile backend lockfiles to fix pydantic-core drift (NFR-009)`, merged, current develop tip `f8d06ad58`) partially moved the baseline — see *Scope*.

## Classification

- **Primary class**: docs
- **Secondary class(es)**: refactor
- **Rationale**: the sole deliverable is a written analysis report under `spec/analysis/`; the issue explicitly excludes code/config changes. The follow-up *implementation* (separate issue) would be `refactor`/`infra`.

## Scope

- **In scope**: an audit report (`spec/analysis/python-dependency-management-audit.md`) that (1) inventories every place backend Python deps are declared/pinned/consumed/updated, (2) determines whether a single source of truth exists and where truth actually lives for the prod image, CI, `pip-audit`, and Renovate, (3) maps the drift surface, (4) checks NFR-009 conformance point by point, and (5) recommends a target model (evaluating Option A pyproject-SSOT+compiled-lock, Option B pyproject-only, Option C uv/poetry) with a follow-up implementation outline.
- **Out of scope**: any code/config change (Dockerfile, renovate.json5, pyproject.toml, CI); frontend `package-lock.json`; `src/knowledge-service` deps; `docs/requirements.txt`. Implementation of the recommended model is a **separate** issue/PR.

## Baseline re-verification (current develop `f8d06ad58`, post-#563)

The issue body was authored against the primary checkout at `35eb32a57` (one commit behind origin). PR #563 has since landed and **partially addressed two observed-facts** — the report MUST re-baseline against current develop:

- ✅ `requirements-dev.txt` **now exists** (issue body said it was missing).
- ✅ `requirements.txt` now carries a proper `pip-compile` autogenerated header (issue body said it only "looks" compiled).

**The architectural core is nonetheless intact and unresolved:**

- ❌ Prod build still installs from pyproject: `src/backend/Dockerfile:52` → `pip install --no-cache-dir .`; dev stage `:38` → `pip install -e ".[dev]"`.
- ❌ CI still installs from pyproject: `.github/workflows/backend.yml:34,51` → `pip install -e ".[dev]"` (not `-r requirements.txt`).
- ❌ Renovate still manages the *lock*, not the *source*: `renovate.json5` targets the `pip_requirements` manager (lines 40/56/68); no PEP 621 manager, no `lockFileMaintenance`/`postUpdateOptions` pip-compile wiring visible.

→ #563 fixed the *symptom* (recompiled the lock, added header + dev lock) but the compiled lock is still consumed by **no** build/CI step, and Renovate still moves pins in a file the build never reads while `pyproject.toml` `>=` bounds stay put. The two-source drift the issue targets remains.

## Route

- **Decision**: direct
- **Rationale**: one coherent outcome (a single audit report), a single PR strand, no new/retargeted roadmap item → direct. Does not span multiple goal outcomes.
- **Pipeline hand-off**: n/a

## Requirements gate

- **Decision**: operator override (recorded here), no `requirements-elicit` run.
- **Rationale**: the issue body is an author-detailed, precise requirement artefact (explicit scope, deliverable path, and three enumerated options A/B/C with trade-off dimensions). Understanding meets `τ_high` without a separate elicitation round. Operator confirmed on 2026-07-12.

## Work packages

### P1 — Author the Python dependency-management audit report

- **Problem statement**: Produce the investigation report commissioned by #566: inventory the backend Python dependency flow, determine where the single source of truth actually lives, map the drift surface (reproduce the #563 class of failure conceptually), check NFR-009 conformance, and recommend a target model.
- **Acceptance criteria**:
  1. Report file exists at `spec/analysis/python-dependency-management-audit.md`.
  2. Contains a dependency-flow inventory + a "where truth lives" table covering (a) prod image, (b) CI, (c) `pip-audit`, (d) Renovate — each with file:line evidence.
  3. Lists every NFR-009 deviation (§119/120/124–125) with file:line, and explicitly reflects the post-#563 baseline (dev lock now present; lock still consumed by no build/CI step).
  4. Distinguishes direct/abstract deps (`pyproject.toml`) from the transitively pinned lock (`requirements*.txt`) and clarifies each file's intended role.
  5. Evaluates Option A (pyproject-SSOT + compiled lock consumed everywhere + Renovate PEP 621 manager & pip-compile lockfile maintenance), Option B (pyproject-only, drop the lock), and Option C (migrate to uv/uv.lock or Poetry), with trade-offs on reproducibility, `pip-audit` accuracy, CI lockfile-integrity checks, and Renovate ergonomics.
  6. States a recommended option with a rough follow-up implementation outline (implementation explicitly deferred to a separate issue/PR).
  7. Follows `spec/style-guides/DOCS.md` conventions where applicable; prose in the repo's analysis-report language.
- **Touched files / artifacts**: writes `spec/analysis/python-dependency-management-audit.md` (new). Read-only over: `src/backend/pyproject.toml`, `src/backend/requirements.txt`, `src/backend/requirements-dev.txt`, `src/backend/Dockerfile`, `.github/workflows/*.yml`, `renovate.json5`, `.github/renovate-pins.yaml`, `spec/nfr/NFR-009_Dependency-Management.md`.
- **Specialist**: no matching specialised agent — generalist remediation (`general-purpose`). Closest catalog candidates are read-only and write nothing to disk (`tech-stack-fitness-reviewer`, `tech-stack-drift-reviewer`) or scoped to CVE/license scanning, explicitly not Renovate/source-of-truth analysis (`nolte-engineering:dependency-audit`). Operator confirmed `general-purpose` on 2026-07-12.
- **Depends on**: none

## Dependency ordering

P1 (single package, no dependencies).

## Risks

- **Stale-baseline risk** — the issue body predates #563. *Mitigation*: the report MUST re-baseline against current develop `f8d06ad58` (captured in *Baseline re-verification* above); the agent is instructed to read live files, not the issue body's observed-facts.
- **Recommendation over-reach** — the report proposes changes but this issue is analysis-only. *Mitigation*: acceptance criterion 6 defers implementation to a separate issue/PR; no code/config edits in this PR.
- **No security-sensitive paths touched** — the change adds one Markdown report; `code-security-reviewer`/`security-review` not required for the diff.

## Open questions

none — scope, classification, requirements override, route, and specialist all confirmed with the operator on 2026-07-12.

## Dispatch log

2026-07-12 P1 dispatched to general-purpose (generalist remediation) — report written to `spec/analysis/python-dependency-management-audit.md` (159 lines). Verdict: no single source of truth; every consumer installs from abstract `pyproject.toml` while Renovate edits only the never-consumed lock. 5 open NFR-009 deviations (§2.1 no upper bounds, §2.3/§6.1 consumers install from pyproject not lock, §4.1 no pip-audit, §4.3 no pip-licenses, §6.1 Renovate not on pep621) + 1 spec-internal contradiction (§5.2 vs §2.3/§6.1); 2 items already closed by #563 (pip-compile header, requirements-dev.txt). Recommended: Option A (pyproject SSOT + compiled lock consumed everywhere + Renovate pep621/pipCompileOutput).
