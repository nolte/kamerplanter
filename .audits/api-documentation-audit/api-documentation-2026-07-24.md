# API Documentation Audit

Scope: /home/nolte/repos/github/kamerplanter, HTTP API: detected (FastAPI >= 0.115, 47 router modules, ~281 operations under `src/backend/app/api/v1/`), 0 OpenAPI documents materialised (skipped: CI curl export — requires a live ArangoDB + backend stack, not started per read-only guardrail)
Trigger: periodic (operator-requested review)
Linter: none available — static checks only (spectral not installed)
Severity floor: Info
Git revision: 41df9a1fc9aeb65eba4c6478314b5ab8a64cb876
Prior artifact: none (first audit)

## Verdict
**fail** — Critical: 10, Warning: 5, Suggestion: 1, Info: 3

## Repository level
- Document presence: **CRITICAL** — HTTP API shipped (FastAPI, 281 operations), no discoverable OpenAPI document: neither checked in at a conventional location nor materialisable via a documented local export command. The document exists only at runtime, generated by FastAPI at `src/backend/app/main.py:182-189` (`openapi_url="/api/v1/openapi.json"`).
- Export command (code-first): **CRITICAL: missing.** The only export in the repository is `.github/workflows/security-nuclei-pr.yml:146-149` — a `curl` against a live backend inside that job's docker-compose stack, built to feed the Nuclei DAST scan, not a docs-export workflow. No Taskfile target, script, or README-documented command reproduces the published document locally; the auditable artifact cannot be reproduced.
- CI lint gate: **Warning: absent.** No spectral or equivalent OpenAPI linter in `.github/workflows/` (14 workflows), `.pre-commit-config.yaml`, `Taskfile.yaml`, or `package.json`.
- Drift wiring (docs-freshness "API reference vs code" / CI re-export diff): **Warning: absent.** The hand-written API docs under `docs/{de,en}/api/` are manually maintained and never checked against the schema; `docs/de/reference/api-reference.md:3-4` notes the mkdocstrings auto-generation path is "not yet fully configured". The Nuclei-CI export is single-shot and discarded, never diffed against a baseline.

## /api/v1/openapi.json (runtime-only)  (OpenAPI 3.1, code-first, discovered via source inspection — `FastAPI()` constructor + `docs/de/architecture/backend.md:147`)

Not materialised — findings derived by static AST inspection of the 47 `router.py` files plus `main.py`, `settings.py`, `common/auth.py`, `common/exceptions.py`, `common/error_handlers.py`. Attribution is `file:line`.

### Critical
- `info.description` missing (MUST): `FastAPI(...)` at `src/backend/app/main.py:182-189` passes no `description=`. Title (`"Kamerplanter API"`) and version (`"1.0.0"`, `src/backend/app/config/settings.py:23-24`) are present and non-placeholder.
- All 86 used tags undeclared at top level (MUST): routers use 86 distinct tags (`activities`, `species`, `admin-platform`, `ki-assistent-public`, …), but `FastAPI()` declares no `openapi_tags` — zero tags have a top-level declaration with a `description`.
- 182/281 operations (65%) have no summary (MUST): neither an explicit `summary=` (only 1 file uses it) nor a function docstring, so FastAPI emits no summary at all. Examples: `src/backend/app/api/v1/activities/router.py:15` (GET), `:34` (POST), `:44` (GET /{key}), `:52` (PUT /{key}), `:63` (DELETE /{key}); `src/backend/app/api/v1/activity_plans/router.py:72` (POST /generate).
- 164/164 path parameters (100%) without a description (MUST): all are bare function arguments (`key: str`) with zero use of `Path(...)` in any router. `required` is implicitly correct for path params. Examples: `activities/router.py:44`, `activity_plans/router.py:127`, `admin/oidc_providers/router.py:79`.
- 19/22 explicit `Query(...)` parameters without a description (MUST). Examples: `activities/router.py:15` (`scope`), `auth/router.py:223` (`code`/`state`/`error`), `care_reminders/router.py:29` (`species_name`/`botanical_family`), `care_reminders/router.py:83` (`reminder_type`/`limit`).
- ~20 operations return untyped ad-hoc dicts with no derivable success schema (MUST): no `response_model` and no typed return annotation. Examples: `companion_planting/router.py:36` (POST /compatible → `{"status": "created"}`), `companion_planting/router.py:47` (POST /incompatible), `family_relationships/router.py:35` (POST /pest-risk). (Legitimate schema-less cases like the `RedirectResponse` at `auth/router.py:209` are excluded.)
- Error status codes undocumented across effectively all 281 operations (MUST): `responses=` is used 0 times in all 47 routers. Business errors are raised as `KamerplanterError` subclasses (`common/exceptions.py`: `NotFoundError`→404, `DuplicateError`→409, `ValidationError`→422, `UnauthorizedError`→401, 20+ subclasses) and translated by global handlers (`common/error_handlers.py:25,57,81`) that the schema generator cannot see; 44 direct `raise HTTPException` router call sites are equally undocumented. Every get/put/delete-by-key that can 404, every duplicate-409, every auth-gated 401/403 is invisible in the schema. The error **body shape** is owned by `spec/project/api-error-handling/` — verify it via `api-error-check`, not here.
- No `components.securitySchemes` and no `security` requirements despite an authenticated API (MUST): 33/47 routers gate operations via `Depends(get_current_user)` / `Depends(require_permission)` (`common/auth.py:15-20`), but auth is a raw `Header(default=None)` parsed manually — none of FastAPI's security utility classes (`HTTPBearer`, `OAuth2PasswordBearer`, `APIKeyHeader`) are used, so the generated document carries zero security schemes and zero per-operation requirements; Swagger UI would have no "Authorize" control for ~230+ Bearer-protected operations.
- Deliberately public operations not recognisable as such (MUST): the 14 routers without a visible auth dependency (`health`, `glossar`, `calculations`, `recognition`, `ki_assistent` public paths, `mcp`, …) carry no explicit `security: []`, so a document consumer cannot distinguish "deliberately public" from "forgotten security requirement".

### Warning
- `info.contact` / `info.license` missing (SHOULD): no `contact=` / `license_info=` kwarg on `FastAPI()`.
- `servers` entries with per-environment descriptions missing (SHOULD): no `servers=` kwarg, no `root_path` configuration.
- No request/response examples anywhere (SHOULD): 0/84 Pydantic model files under `domain/` use `json_schema_extra` or `examples=`; no primary-success-response examples exist.
- No CI lint gate over the OpenAPI document (SHOULD) — see repository level.
- No drift wiring (SHOULD): neither the docs-freshness "API reference vs code" category nor a CI re-export diff for the code-first document — see repository level.

### Suggestion
- 0/281 operations set an explicit `operation_id=`: FastAPI's auto-generated IDs are unique (MUST met) but not deliberately authored, degrading generated-client-SDK naming quality. Consider a project-wide `generate_unique_id_function` or explicit IDs on new routes.

### Info
- Flavour: code-first (FastAPI-generated at runtime, OpenAPI 3.1 per FastAPI default — version floor met, unverified against a materialised artifact); discovery via source inspection.
- Spectral not installed in the audit environment — no linter findings recorded.
- No real credentials, tokens, or secrets in examples: AWS-key and live-JWT pattern sweeps over `docs/` and `src/backend/app/` returned zero hits; the only Bearer example (`docs/{de,en}/api/authentication.md:119`) is a truncated placeholder.

## Health
- Documents audited: none materialised; source-level static audit over `src/backend/app/api/v1/**/router.py` (47 files, 281 operations, AST-parsed without app import or execution) plus `main.py`, `settings.py`, `common/auth.py`, `common/exceptions.py`, `common/error_handlers.py`.
- Skipped (with reason): the CI-internal export command (`curl http://127.0.0.1:8000/api/v1/openapi.json`, `.github/workflows/security-nuclei-pr.yml:146-149`) — requires a live ArangoDB + backend stack; not started per the read-only guardrail.
- Discovery methods used: conventional-location glob/grep sweep (repo root, `docs/`, `api/`, `openapi/`; `site/` is gitignored build output), README/docs grep for a declared document path, workflow/Taskfile/package.json grep for an export command, direct inspection of the `FastAPI()` construction.
- Linter: spectral not installed.

## Recommended remediation order (advisory — this audit changes nothing)

1. **Make the document reproducible** (clears both repository-level Criticals): add a local export command (e.g. a Taskfile target that imports the app and dumps `app.openapi()` to JSON without a running stack) and document it; optionally check the export in or diff it in CI (also clears the drift Warning).
2. **Adopt FastAPI security utilities** (`HTTPBearer` or equivalent) in `common/auth.py` so `securitySchemes` + per-operation `security` are generated; mark public routes with explicit empty security.
3. **Document error responses**: a shared `responses=` map (401/403/404/409/422 as applicable) applied per router or via a router factory, pointing at the `spec/project/api-error-handling/` body contract.
4. **Close the per-operation gaps**: `openapi_tags` on the app, summaries/docstrings for the 182 bare operations, `Path(..., description=)`/`Query(..., description=)`, `response_model` for the ~20 ad-hoc-dict operations, `info.description`.
5. **SHOULDs**: contact/license/servers metadata, examples on primary models, a spectral (or equivalent) CI lint gate.
