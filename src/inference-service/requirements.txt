fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18
onnxruntime>=1.20.0
numpy>=2.0.0
pillow>=11.0.0
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
structlog>=24.0.0
# #777 — optional error tracking; no-op without SENTRY_DSN.
sentry-sdk[fastapi]>=2.20.0
# REQ-038 PlantCV phenotype pipeline is an OPTIONAL extra (pyproject [cv]); it is
# intentionally NOT pinned here so the base image stays lean. Install `plantcv`
# only in deployments that want the phenotype panel; the service runs without it.
