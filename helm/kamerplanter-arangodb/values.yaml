image:
  repository: arangodb
  tag: "3.11"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8529

rootPassword: rootpassword

persistence:
  enabled: true
  size: 5Gi
  storageClass: ""

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi
