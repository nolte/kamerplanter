apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kamerplanter.fullname" . }}-backend-config
  labels:
    {{- include "kamerplanter.backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
