apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kamerplanter-backend.fullname" . }}-config
  labels:
    {{- include "kamerplanter-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
