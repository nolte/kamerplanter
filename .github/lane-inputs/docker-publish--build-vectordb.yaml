# Recorded by scripts/ci/lane_inputs.py — what this job READS, so its relevance
# filter can be held against it (#1596). Do not edit `reads` by hand: the CI lane
# .github/workflows/lane-inputs.yml replays the `invocations` below and fails when
# its recording differs from this file (#1683). The guard is
# src/backend/tests/unit/guards/test_lane_filters_cover_measured_inputs.py.
schema: 2
workflow: docker-publish.yml
job: build-vectordb
gate:
  kind: paths-filter
  filter: vectordb
status: derived
measured_on: '2026-09-25'
measured_at_commit: c58248f03e803375a3f8a0919ff9717721c8f0f4
replay_seconds: 1
job_spec_sha256: 27c34ea45cd9a85402182e3dabe83d2ce1ccf58977bbe6154382d881c38f8ec2
invocations:
- command: derive-docker --context docker/vectordb --dockerfile docker/vectordb/Dockerfile
  cwd: .
  recorder: docker build context probe (FROM scratch, COPY . — the daemon applies .dockerignore) narrowed to the Dockerfile's
    COPY/ADD sources
  copy_sources: []
  context_files_after_dockerignore: 0
subprocesses: []
accepted_gaps: []
reads:
- docker/vectordb/.dockerignore
- docker/vectordb/Dockerfile
test_modules: []
readers: {}
