# Recorded by scripts/ci/lane_inputs.py — what this job READS, so its relevance
# filter can be held against it (#1596). Do not edit `reads` by hand: the CI lane
# .github/workflows/lane-inputs.yml replays the `invocations` below and fails when
# its recording differs from this file (#1683). The guard is
# src/backend/tests/unit/guards/test_lane_filters_cover_measured_inputs.py.
schema: 2
workflow: security-nuclei-templates.yml
job: validate
gate:
  kind: on-paths
status: measured
measured_on: '2026-09-24'
measured_at_commit: 6b694a459dff9ece4b649cb6dbdeb79593f0a531
job_spec_sha256: ad271090bee63f1601810ebfc5157029abd2e7014580ca4be688446627962924
invocations:
- command: yq e .nuclei_templates_commit .github/renovate-pins.yaml
  cwd: .
  recorder: strace -ff -y -z
  exit_code: 0
  env: []
  untracked_reads_dropped: 0
- command: yq e .nuclei_version .github/renovate-pins.yaml
  cwd: .
  recorder: strace -ff -y -z
  exit_code: 0
  env: []
  untracked_reads_dropped: 0
- command: bash -c 'find tests/security/nuclei-templates -name '"'"'*.yaml'"'"' -type f | wc -l'
  cwd: .
  recorder: strace -ff -y -z
  exit_code: 0
  env: []
  untracked_reads_dropped: 1
- command: nuclei -validate -t tests/security/nuclei-templates/
  cwd: .
  recorder: strace -ff -y -z
  exit_code: 0
  env: []
  untracked_reads_dropped: 1
subprocesses:
- bash
- ciphers
- find
- nuclei
- version
- wc
- yq
accepted_gaps: []
reads:
- .github/renovate-pins.yaml
- tests/security/nuclei-templates/
- tests/security/nuclei-templates/kamerplanter-cors-misconfig.yaml
- tests/security/nuclei-templates/kamerplanter-debug-endpoints.yaml
- tests/security/nuclei-templates/kamerplanter-device-pairing-bruteforce.yaml
- tests/security/nuclei-templates/kamerplanter-jwt-leak.yaml
- tests/security/nuclei-templates/kamerplanter-security-headers.yaml
- tests/security/nuclei-templates/kamerplanter-source-map.yaml
- tests/security/nuclei-templates/kamerplanter-tenant-leak.yaml
