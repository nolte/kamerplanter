# Recorded by scripts/ci/lane_inputs.py — what this job READS, so its relevance
# filter can be held against it (#1596). Do not edit `reads` by hand: re-run the
# `invocations` below. The guard is
# src/backend/tests/unit/guards/test_lane_filters_cover_measured_inputs.py.
schema: 2
workflow: docker-publish.yml
job: update-release-assets
gate:
  kind: on-paths
status: measured
measured_on: '2026-09-23'
measured_at_commit: bc7de7c58e5997fd9e6b202e6b67846bf9bc38c0
job_spec_sha256: 47b77e553245cc387ebe639dbd4b14d7bbc7d26501c87d7179594259f3ba48a3
invocations:
- command: bash -c 'VERSION_NUM="${VERSION#v}"; sed "s/__VERSION__/${VERSION_NUM}/g" docker-compose.release.yml > /tmp/claude-1000/-home-nolte-repos-github-kamerplanter/97003d13-043f-40e9-b936-eb80607b2daa/scratchpad/docker-compose-x.yml;
    cp .env.example /tmp/claude-1000/-home-nolte-repos-github-kamerplanter/97003d13-043f-40e9-b936-eb80607b2daa/scratchpad/.env.example-x'
  cwd: .
  recorder: strace -ff -y -z
  exit_code: 0
  env:
  - VERSION=v0.0.0
  untracked_reads_dropped: 0
subprocesses:
- bash
- cp
- sed
accepted_gaps: []
reads:
- .env.example
- docker-compose.release.yml
unrecorded_invocations:
- command: scripts/ci/publish_release_asset.sh "$VERSION" bytes "docker-compose-${VERSION_NUM}.yml" ".env.example-${VERSION_NUM}"
  reason: Runs only on a release tag against the GitHub release of that tag (gh auth, an existing release). Its inputs are
    the two files the recorded `bash -c` step templated from docker-compose.release.yml and .env.example, both in the on:-level
    filter; the script itself is too.
