# ── Backend ──────────────────────────────────────────────────────────────────
# Lint, format, the pytest tiers, the OpenAPI export/lint lane and the
# dependency-lock targets. The lint/test/format targets are the exact
# invocations Backend CI (backend.yml) runs.
#
# Included from the root Taskfile.yaml with `flatten: true` — task names here
# are global and MUST NOT collide with other .taskfiles/*.yaml.

version: "3"

tasks:
  lint:backend:
    desc: Run ruff linter on backend code
    dir: src/backend
    cmds:
      - ruff check .

  format:backend:
    desc: Check backend formatting with ruff (no rewrite) — the CI gate's exact invocation
    dir: src/backend
    cmds:
      - ruff format --check .

  test:backend:
    desc: Run the full backend pytest suite — the three CI tiers plus tests/integration/, which needs a local ArangoDB
    dir: src/backend
    cmds:
      - python -m pytest tests/ {{.CLI_ARGS}}

  # The tiers CI runs, as their own targets. `test:backend` above stays the full
  # local suite; these are what the pipeline invokes, so the pipeline and a
  # contributor can run the identical thing.
  #
  # CI runs unit, contracts and api — every tier that needs no service. Until
  # 2026-08-05 the api tier ran in no gate at all, on the stated grounds that
  # "the remaining tiers need services no runner provides", which was never true
  # of this one. Measured with no ArangoDB, no Valkey and no S3 listening: 480
  # passed, 0 skipped, ~60 s, against the ~5 min the unit tier already costs.
  # Among the tests it left unprotected were the cross-tenant negative tests on
  # the diary endpoints. A test that never runs protects nothing.
  #
  # Ten modules used to sit loose directly under `tests/`, in no tier directory
  # and therefore in no target and no gate either — 169 tests, ~1.5 s, no
  # services, among them the CSRF and OAuth-engine ones. On 2026-08-05 they were
  # moved into the matching `tests/unit/` subtrees instead of getting a target of
  # their own: they are plain unit tests (no TestClient, no fixture of their own)
  # and the tier they belong to was already gated. `tests/` root now holds only
  # conftest.py and __init__.py, so a new module cannot silently fall out of CI
  # by being dropped in the wrong place — every test file lives in a tier.
  #
  # `tests/integration/` stays out and therefore runs LOCALLY ONLY — say that
  # out loud, or it looks covered. It needs a real ArangoDB on localhost:8529,
  # which no runner provides. It would not go red without one: every module is
  # `skipif(not ARANGO_AVAILABLE)`, so on a runner it would report green having
  # skipped 29 of 36 cases. A green gate that measured nothing is worse than an
  # absent one. Run it against a dev stack (`task dev:core`, then
  # `python -m pytest tests/integration/`) before changing repositories or AQL.
  #
  # `tests/unit/` and `tests/api/` carry a datastore guard (#978,
  # src/backend/tests/support/db_guard.py): any ArangoDB, TimescaleDB or Valkey
  # connection raises on the spot, naming the provider chain that asked for it.
  # Without it such a test passes on a machine with the dev stack up — against
  # the dev database — and fails in CI after ~18 s of connection retries.
  # `tests/contracts/` has no guard: it compares enums and JSON, builds no
  # service, and measured zero connection attempts. `tests/integration/` must
  # connect and is deliberately untouched.
  test:backend:unit:
    desc: Backend unit tests — the CI gate's exact invocation
    dir: src/backend
    cmds:
      - python -m pytest tests/unit/ -v --tb=short {{.CLI_ARGS}}

  test:backend:contracts:
    desc: Backend contract tests (enum sync, i18n completeness) — the CI gate's exact invocation
    dir: src/backend
    cmds:
      - python -m pytest tests/contracts/ -v --tb=short {{.CLI_ARGS}}

  test:backend:api:
    desc: Backend API tests (routers via TestClient against fakes, no services) — the CI gate's exact invocation
    dir: src/backend
    cmds:
      - python -m pytest tests/api/ -v --tb=short {{.CLI_ARGS}}

  openapi:export:
    desc: |
      Export the OpenAPI document (src/backend/openapi.json, gitignored) from
      the FastAPI app — no running stack required. The document is a build
      artifact, never checked in: the api-docs CI lane re-exports and lints it
      per backend change, and the release build attaches it as a release
      asset (spec/project/api-documentation/).
    dir: src/backend
    cmds:
      - python -m scripts.export_openapi --out openapi.json {{.CLI_ARGS}}

  openapi:lint:
    desc: |
      Export the OpenAPI document and lint it with Spectral — the api-docs CI
      lane's exact invocation (same pinned spectral-cli version, same ruleset,
      same --fail-severity error).
    deps: [openapi:export]
    dir: src/backend
    cmds:
      - npx --yes @stoplight/spectral-cli@6.16.3 lint --ruleset ../../.spectral.yaml --fail-severity error openapi.json

  deps:toolchain:
    desc: |
      Install the pinned toolchain that compiles the backend locks
      (.github/pip-tools-requirements.txt) into the ACTIVE Python 3.14
      environment. `deps:compile` and `deps:check` both declare this task as a
      prerequisite, so you no longer have to remember it — but that also means
      either of them will install this set into whatever environment is active.
      Prefer a throwaway virtualenv.

      Only two entries of that file also appear in the backend's own locks, and
      both are held equal to them on purpose: click at 8.4.2 and packaging at
      26.2. So running this inside a backend virtualenv no longer leaves it on a
      packaging the lock does not pin — it did until 2026-08-31, when this file
      pinned packaging 26.3 against a lock that pins 26.2.

      Every pin in that file is load-bearing, but not all for the same reason —
      the file itself says which does what, measured rather than assumed.
      pip-compile reconstructs the header line it writes into each lock from
      *click's* parameter metadata, so click 8.4.2 -> 8.5.0 alone rewrote both
      committed locks' headers and reddened the gate on every backend pull
      request (#1296); setuptools and wheel, by contrast, never touch this
      project's own metadata (pip-tools parses its pyproject.toml statically) and
      matter only because `--no-build-isolation` makes them build the two
      sdist-only dependencies whose metadata feeds the resolution.
    cmds:
      - python -m pip install --no-deps -r .github/pip-tools-requirements.txt
      - python -m pip check

  deps:compile:
    desc: |
      Recompile the backend dependency locks (requirements.txt +
      requirements-dev.txt) from src/backend/pyproject.toml — the single source of
      truth (NFR-009 §6.1). Run this after editing [project].dependencies or the
      dev extra, then commit the regenerated locks.

      These two `pip-compile` lines are the ONLY copy of the lock-compiling flags
      in the repository: backend.yml's "Lock staleness" job runs `task
      deps:check`, which runs this task, so CI and a contributor cannot drift
      apart on a flag. They used to be duplicated inline in the workflow, and
      `.taskfiles/backend.yaml` was not even in that workflow's `paths:` — a pull
      request dropping `--no-strip-extras` here would have triggered no backend
      job at all.

      Why each flag: `--generate-hashes` is NFR-009 §2.3. `--no-strip-extras` is
      #903 (Renovate cannot map `uvicorn[standard]` to a lock entry that stripped
      the extra) and a deliberate opt-out of pip-tools 8.0's future default.
      `--no-build-isolation` is #1296: two dependencies ship as sdists only
      (aquacropeto, http-ece via pywebpush) and pip builds their metadata during
      resolution, which decides what lands in the lock; with build isolation pip
      would install each sdist's build backend fresh from PyPI, unpinned, so the
      flag is what makes the pinned setuptools/wheel the ones that render that
      metadata. It is recorded in each lock's header, which is why adding it
      recompiled both.
    deps: [deps:toolchain]
    dir: src/backend
    cmds:
      - pip-compile --no-build-isolation --generate-hashes --output-file=requirements.txt --no-strip-extras pyproject.toml
      - pip-compile --no-build-isolation --generate-hashes --extra=dev --output-file=requirements-dev.txt --no-strip-extras pyproject.toml

  deps:check:
    desc: |
      Lock-staleness gate — and literally the thing CI runs (backend.yml "Lock
      staleness" invokes this task): recompile both locks via `deps:compile`,
      then fail if the committed files differ. Catches a pyproject.toml bump that
      was not re-compiled, or a hand-edited lock (NFR-009 §2.3). On failure the
      regenerated locks are left in the working tree — that diff is precisely
      what `task deps:compile` produced, so commit it if the bump was intentional.

      Carries no `pip-compile` invocation of its own on purpose: it delegates to
      `deps:compile` so the gate and the fix cannot be run with different flags.
      `deps:compile` in turn pulls in `deps:toolchain`, so a bare `task
      deps:check` installs the pinned toolchain first.
    deps: [deps:compile]
    dir: src/backend
    cmds:
      - git diff --exit-code -- requirements.txt requirements-dev.txt
